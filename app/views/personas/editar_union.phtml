<h2>Editar unión</h2>

<p>
    <strong><?= $persona1->nombre ?>
    <?= $persona1->apellidos ?></strong>

    y

    <strong><?= $persona2->nombre ?>
    <?= $persona2->apellidos ?></strong>
</p>

<p>
    Tipo de unión:
    <strong><?= ucfirst($union->tipo) ?></strong>
</p>

<form method="post">

    <div>
        <label for="fecha_inicio">
            Fecha de inicio
        </label>

        <input
            type="date"
            id="fecha_inicio"
            name="fecha_inicio"
            value="<?= $union->fecha_inicio ?>"
        >
    </div>

    <div>
        <label for="lugar">
            Lugar
        </label>

        <input
            type="text"
            id="lugar"
            name="lugar"
            value="<?= $union->lugar ?>"
        >
    </div>

    <div>
        <label for="notas">
            Notas
        </label>

        <textarea
            id="notas"
            name="notas"
            rows="5"
        ><?= $union->notas ?></textarea>
    </div>

    <div>
        <button type="submit">
            Guardar cambios
        </button>

        <?= Html::link(
            'personas/uniones/' . $union->persona1_id,
            'Cancelar'
        ) ?>
    </div>

</form>